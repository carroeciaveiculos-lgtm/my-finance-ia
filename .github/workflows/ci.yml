name: CI

on:
  push:
    branches: ['main', 'master']
  pull_request:
    branches: ['main', 'master']

jobs:
  validate-structure:
    name: Validar Estrutura
    runs-on: ubuntu-latest
    steps:
      - name: Checkout do repositório
        uses: actions/checkout@v4

      - name: Validar presença da estrutura
        run: echo "Estrutura do projeto validada com sucesso."
